
def example_pace_inference(info):
    accelerations = info["accelerations"]

    def inference(index, peak):
        a_earth = accelerations[peak]
        # index 从0 到 步数
        # peak 从0 到 总数据数（可以直接用来访问info中的数据

predict(None, )